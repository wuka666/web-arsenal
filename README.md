# Web 灵感弹药库（Web Arsenal）

> 把「看到的好效果」，变成「能跑、能调、能直接喂给 AI」的弹药。
> **71 条零依赖网页动效 / 交互素材**，每条配可运行 Demo + 可调参数 + 大白话说明 +
> 完整可复刻的 AI 提示词。

## 这是什么

一个面向「用 AI 辅助做网页的人」的素材库（vibe coding 弹药箱）。它和普通收藏夹、
灵感站的区别是：**效果即代码**——看到的东西不只是截图和文字，而是一条能直接跑、
能调参、能复制给别人用的成品弹药。

- **效果即代码**：每条素材都是 `assets/demos/` 里可直接双击运行的原生 HTML/CSS/JS，
  零框架、零 CDN、可离线
- **小白词检索**：标签不说黑话（「手机端网页」「鼠标悬停」），按
  文字动画 / 动画 / 组件 / 背景 / 方案 五大分类组织
- **提示词直达**：每条配一段完整提示词（效果 + 用法示例 + 关键参数 + 集成步骤），
  复制给 AI 就能照做
- **参数联动**：滑杆 / 开关 / 取色器调完之后，复制出来的提示词和代码就是你调好的
  那一版，不存在「演示调过、复制出来还是默认值」的错位

## 为什么做（先读：这不是抄袭）

做这个库的动机是**学习拆解 + 复用沉淀**：把在好网站上看到、在项目里用过的交互手法，
用自己的理解重写一遍、讲清楚、存下来，下次做网页直接取用。它不是素材的搬运，而是
「看到 → 拆解 → 复刻 → 沉淀」的完整消化过程。

### 来源与原创声明

- 本库 **71 条素材逐条标注「来源 + 分析日期」**，字段就写在 `data/素材.js` 里，可追溯
- 本库所有代码均为**观察交互手法后的原创复刻 / 自写**，原生 HTML/CSS/JS 零依赖重写，
  未复制任何网站源码、字体、图片或商业素材
- 借鉴对象包括：ReactBits（MIT 许可，已逐条标注）、Stripe / Apple / stateofaidesign
  等官网的交互手法，以及视频资料中的通用交互模式
- 网页的「交互思路 / 动效手法」属于通用设计语言；本库展示的是自己的实现，不是
  别人的代码或设计资产
- 如果你认为某条内容涉及你的作品且不妥：提一个 Issue 说明，确认后立即下架

## 快速开始

```text
双击 index.html         # 素材数据内嵌为 JS 变量，file:// 直接可跑，无需服务器
```

也可以在项目目录起任意静态服务器打开。在线演示（GitHub Pages）正在部署。

## 单条素材 = 六件套

每条素材都包含：可运行演示、参数面板、大白话效果说明、用法、完整提示词、可复制代码、
来源标注。新增素材的填写标准见 `01-素材条目模板.md`。

## 全库索引（71 条）

### 文字动画（5）

- `w001` 粒子文字
- `r001` 文字遮罩
- `soa03` 编辑型大序号分章
- `soa07` 文字逐行揭示
- `soa08` 英雄区大字号排版

### 动画（15）

- `w003` 翻页动画
- `w005` 悬停反馈
- `a103` 产品卡悬停
- `s202` 悬浮边框
- `v104` 点击爆散
- `v107` 数字滚动
- `v109` 卡片翻面
- `v110` 汉堡变叉
- `v111` 卡片抽走
- `v116` 弹性动效
- `v120` 转场衔接
- `v121` 状态收尾
- `v125` 微光动效
- `v129` 网格吸附
- `soa01` 滚动揭示入场

### 特效（1）

- `v140` 光标探照揭示 Hero

### 组件（40）

- `w004` 墨渍按钮
- `s204` logo 墙
- `v101` 按压回弹
- `v102` 弹性开关
- `v103` 水波按钮
- `v105` 勾选动效
- `v106` 液态滑块
- `v108` 骨架落位
- `v112` 底部抽屉
- `v113` 半屏停留
- `v114` 全屏展开
- `v115` 背景处理
- `v117` 拖拽关闭
- `v118` 档位吸附
- `v119` 嵌套抽屉
- `v122` 模糊进度
- `v123` 明确进度
- `v124` 环形进度
- `v126` 按钮加载
- `v127` 页面加载
- `soa02` 数字滚动统计(滚动触发)
- `soa04` 滚动进度指示
- `soa05` 订阅表单微交互
- `soa06` 粘性章节导航
- `v132` 普通单选下拉
- `v133` 分组下拉
- `v134` 可搜索组合框
- `v135` 拆分按钮
- `v136` 日期选择器
- `v137` 级联选择器
- `v138` 多选下拉
- `v139` 大型菜单
- `v142` 悬浮吸顶导航
- `v143` 侧边栏导航
- `v144` 面包屑导航
- `v145` 二级下拉导航
- `v146` 汉堡菜单导航
- `v147` 全屏遮罩导航
- `v148` 锚点导航
- `v149` 滚动收缩导航

### 背景（5）

- `w002` 细雨
- `w006` 3D 背景
- `s201` 绸缎渐变
- `v128` 流体融合
- `v130` 3D 沉浸滚动

### 方案（5）

- `a101` 双按钮
- `a102` 视觉重量
- `s203` 首页动线
- `v131` 动效节奏
- `v141` Interactive Discovery 整站首屏

> 完整可交互浏览入口是 `index.html`；条目 ID 规则：`w` = 自建工作台、`a` = Apple 分析、
> `s` = Stripe 分析、`r` = ReactBits 借鉴、`v` = 视频 / 网站拆解（含 motionsites 系列）、
> `soa` = stateofaidesign 分析。

## 目录结构

```text
web-arsenal/
├── index.html               # 画廊首页（双击即用）
├── assets/demos/            # 每条素材的可运行演示（纯 HTML/CSS/JS）
├── data/
│   ├── 素材.js              # 71 条素材数据（来源 / 说明 / 提示词 / 代码 / 参数）
│   ├── 预设.js              # 15 种排版风格预设
│   └── 案例.js              # 经典整站案例（效果 + 提示词 + 代码三件套）
├── mcp/                     # 可选：本地 MCP 服务器，AI 客户端可直接检索素材
└── 01-素材条目模板.md        # 新增素材的填写模板
```

## 给 AI 客户端用：本地 MCP（可选）

`mcp/` 是一个 stdio MCP 服务器，让 Cursor / VS Code / WorkBuddy 等 MCP 客户端能直接
搜索、读取弹药库素材（ReactBits 也提供同类 MCP 能力）。启用方式：

```text
cd mcp && npm install        # 安装 @modelcontextprotocol/sdk
node server.js               # MCP 客户端把该命令配成 stdio 服务器即可
```

提供 5 个工具：`search_materials`（多维筛选）、`get_material`（完整条目）、
`get_prompt`（只取提示词）、`get_code`（只取可运行 HTML）、`random_material`（找灵感）。

## 技术约定

- 纯原生 HTML/CSS/JS：零依赖、可离线、演示文件双击即跑
- 中文注释，关键行写「为什么」
- 来源必标：没有来源 + 说明的素材不入库（宁缺毋滥）

## Roadmap

- 近期：GitHub Pages 在线演示、素材持续扩容、搜索与推荐
- 中期：经典整站案例库（`data/案例.js`）、AI 素材推荐
- 远期：从「灵感弹药库」走向「定制网页流水线」

## FAQ

**可以直接抄走用吗？** 可以。代码部分按 MIT 许可自由使用（商用也行），保留版权声明即可；
不要把仓库里的效果说成完全原创的设计即可。

**这些效果是从大网站「扒」来的吗？** 不是。只借鉴交互手法，代码全部自写重写，逐条标注
来源；不复制任何第三方源码、字体、图片或商业素材，详见上方「来源与原创声明」。

**为什么每条素材要配一段「给 AI 的提示词」？** 因为 AI 辅助开发已经成为做网页的主流
工作流。效果 + 关键参数 + 集成步骤写清楚，复制给 AI 就能复刻出同款效果，省去从零描述。

**想提交素材 / 报问题？** 提 Issue 或按 `01-素材条目模板.md` 的标准补充素材。

## 许可

代码部分：MIT License（见 [LICENSE](LICENSE)）。素材来源逐条标注于 `data/素材.js`；
第三方网站名称、商标与设计版权归各自所有者，本仓库仅作学习拆解与原创复刻展示。

## 作者

[wuka666](https://github.com/wuka666) —— AI 应用方向在校生。
这个库是「看到好效果 → 拆解 → 复刻 → 沉淀成可复用弹药」过程的公开产物：
AI 辅助开发，但每条效果都能讲清原理、都能改。
