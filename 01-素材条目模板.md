# 素材条目模板（复制这条填）

标题：

适配端：通用 / PC 端 / 移动端（**单选**）
- **通用** = 375px 下自然堆叠成单栏、每段仍完整可读（单栏 / 居中 / 纵向叙事 / 全屏氛围 / 全屏地图）
- **PC 端** = ① 窄屏破版（横向溢出）② 窄屏仍保留并排多栏被压碎 ③ 语义依赖并排对照（左右分屏 / 两栏对照 / 竖排索引+正文）④ 核心交互依赖 hover 或光标
- **移动端** = 移动端专属形态（底部标签栏 / 底部 sheet / 侧滑操作 / 长按菜单 / 滚轮选择器）
- 判定工具：`node scripts/check_adapt.js [--materials]`（375×812 iframe 真机视口实测，报横向溢出与并排多栏；判定前先跑它，别凭感觉）

元素：动作 / 输入 / 导航 / 反馈 / 数据 / 容器布局 / 媒体 / 文字 / 动效 / 背景氛围（**单选**）

风格：极简瑞士 / 科技未来 / 暗色 / iOS 原生 / 品牌海报 / 国风水墨 / 玻璃拟态 / 新拟态软 UI / 粗野·新粗野 / 有机自然 / 复古怀旧 / 童趣黏土 / 编辑杂志（**单选**；没有主导视觉风格就留空，让筛选的「全部」承担该语义）

场景：落地页·发布页 / 官网·品牌站 / 工具·SaaS / 后台·数据看板 / 作品集·叙事 / 内容·阅读 / 电商·预订（**单选**）

> 四维取值都是**闭集**，只能从上面这些词里选（`scripts/check_all.js` 会校验，防止碎片值重新长出来）。
> 四个维度**同级、无上下级**，彼此是交集关系；每个筛选区都自带「全部」= 不约束该维度。
> **方案库没有「元素」**（整站方案不按元素分），只有 适配端 / 风格 / 场景。

标签：用逗号分隔

来源：按宪法规矩 19 的五分类写法（①具体网站「站名 + 域名 + 分析日期」/ ②抖音「来自于抖音」/ ③自研 / ④机制参考 / ⑤方案库拆解）

效果演示：演示文件路径（如 assets/demos/xxx.html，双击能跑；白纸原则——白底只放效果，个别深色氛围效果例外）

（方案类：演示页 = 标题 + 目的 + 步骤 + 示意，一眼看懂，不用调参）

> 需要图片 / 特定素材才能展示的效果：AI 可自行准备素材（canvas 生成 / 本地图 / 扒图），放 assets/，来源与许可写进条目来源字段。

参数：详情页调参面板的数据（滑杆：键/名/最小/最大/步长/默认；开关：键/名/默认；颜色：键/名/选项/默认；下拉：键/名/选项/默认）

搭配：可一起使用的其它素材**标题**（只填素材标题——不填方案名、不填 demo 文件名）

效果说明：大白话——这个效果是什么、什么场景用

用法：大白话——详情页里的滑杆/开关能调什么、代码怎么改

提示词（直接给 AI 的）：完整模板——效果 + 用法示例 + 关键参数（和演示调参面板对齐）+ 集成步骤（一段可粘贴的自然语言提示词）

代码（可复制/库引用）：（短代码直接贴；复杂效果写进 assets/库/ 或 assets/demos/，这里写引用方式）

复用记录：用在哪次项目/接单（空着=还没用，回填）

> 入库标准：效果说明 + 用法 + 完整提示词 + 可运行效果演示（白纸）+ 参数定义（详情页调参），缺一不可。
