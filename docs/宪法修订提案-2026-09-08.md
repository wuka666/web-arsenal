# 宪法修订提案（2026-09-08）

> **状态：已按用户 2026-09-08 决策全部执行完毕。** 下方为执行对照。
>
> 用户 6 条决策：① 认可规矩12 扩为 9 段；② 不拆独立「许可」字段；③ 67 条说明按宪法重做（三把尺子+能怎么改+满两行）；④ 不新增「案例」、补充素材交付二选一+3D打包；⑤ 规矩11 三问与规矩10③冲突由 AI 拍板；⑥ README 分类以代码为准同步。
>
> 执行结果：规矩12 已扩 9 段；规矩10③ 加库引用例外；规矩11 澄清三问+补 3D 打包二选一；
> 规矩1.1/3.4 加第三方 LICENSE 保留；starflow.js 补 MIT 署名头；README 分类同步为 5 类；
> 67 条「效果说明」按 5 类构图框架重写（67/67 满两行+含能怎么改）。

---

## 一、awesome-design-md 扫描结论（作为规矩12 修订依据）

VoltAgent/awesome-design-md 是「从真实网站抽取视觉系统、写成 AI 代理能读的 DESIGN.md」策展集。
本库可直接借鉴的优点：

1. **DESIGN.md 范式**：Google Stitch 提出的纯文本设计系统文档，Markdown 即 LLM 最友好格式，
   扔进项目根目录任何 AI 编码代理就能照着生成视觉一致的 UI。
2. **9 段标准章节**（每个 DESIGN.md 都按此组织）：
   - 视觉主题与氛围 / 色彩板与角色（语义名 + hex + 功能角色）/ 字体规则（完整层级表）/
     组件样式（含 hover/active/disabled 状态）/ 布局原则（间距刻度+栅格+留白）/
     深度与层级（阴影系统+表面层级）/ 该做·不该做 / 响应式行为 / Agent 提示词指南。
3. **语义色 + 功能角色**：如「accent 仅用于最高优先级 CTA」，禁止近似色——正好补本库「设计 Token 法典」短板。
4. **Do's & Don'ts 显式护栏**：本库原「禁忌条款」升级为显式正反清单。
5. **配套 preview.html / preview-dark.html**：每个站点一份 Token 可视化目录（色板/字号比例/按钮/卡片），
   一眼可见设计语言——本库可用现有 `assets/demos/` demo 承担此角色。
6. **Agent 提示词指南**：快速参考卡 + 即拿即用提示词，对应本库「执行令」。

---

## 二、规矩12 修订提案（最新一条，用户已预授权，待确认文字）

**现行**：方案库 `Agent提示词` 遵循 Google Stitch DESIGN.md 范式。格式：效力声明 → 总纲 →
设计 Token 法典 → 组件规范 → 布局法 → 降级条款 → 禁忌条款 → 执行令。

**建议改为**（对齐 awesome-design-md 的 9 段，并把本项目已有字段塞进去）：

> 12. **DESIGN.md 提示词模板（2026-09-07 定，2026-09-08 对齐 awesome-design-md 升级）**：
> 方案库 `Agent提示词` 字段遵循 Google Stitch DESIGN.md 范式（参考
> https://github.com/VoltAgent/awesome-design-md 仓库内的 DESIGN.md 范本，其把真实网站视觉系统抽成 9 段标准章节）。
> 本库 `Agent提示词` 一律按以下 **9 段** 组织（段首即效力声明：本方案唯一设计权威，冲突以 Token 法典为准）：
> 1. **视觉主题与氛围**：设计哲学一句话（这页看起来/感觉是什么）+ 密度/气质/情绪。
> 2. **色彩板与角色**：每个颜色用「语义名 + hex + 功能角色」三元组（如 `accent #FF6B35 仅用于最高优先级 CTA`，禁止近似色）；≥2 个颜色参数。
> 3. **字体规则**：字体族 + 完整层级表（display/heading/body/caption 字号字重行高）。
> 4. **组件规范**：按钮/卡片/输入/导航，含 hover/active/disabled 状态，引用上文 token。
> 5. **布局法**：间距刻度（4/8/12/16…）+ 栅格 + 留白哲学 + 内容落点。
> 6. **深度与层级**：阴影系统 + 表面层级（surface-1/2/3），统一用 token 表达。
> 7. **该做 / 不该做（Do's & Don'ts）**：显式护栏清单 + 反模式（原「禁忌条款」升级）。
> 8. **响应式行为**：断点 + 触控目标尺寸 + 折叠策略（原模板缺失，补齐）。
> 9. **Agent 提示词指南**：快速配色/字号参考卡 + 一段即拿即用提示词，把页面内容套进设计语言输出完整 HTML（原「执行令」升级）。
> - 每个方案须配套「设计 Token 可视化预览」：`assets/demos/` 内 demo 同时承担色板/字号比例/按钮/卡片 一眼可见；暗色方案提供 `preview-dark` 视角。
> - 已有方案（如 s206）已按此格式填写，新方案入库时一律遵循。

---

## 三、开源署名落实（对应用户 ②③：开源、标来源即可、非商用、面向大众）

README 已声明「代码 MIT，商用也行，保留版权声明即可」（FAQ 节），与用户口径一致。
因此**不必拆独立的 `许可` 字段**（原 Q3 撤销）——署名留在 `来源` 字段即可。

仍需补的一处真实合规缺口：**`assets/库/starflow.js` 把 three.js r180 + starflow 的 MIT 许可头整段剥离了**。
MIT 要求保留版权声明，剥离即违规。建议：
- 在 `starflow.js` 文件头部补一行 MIT 署名注释（three.js r180 + win-hao/starflow，均 MIT）。
- 微调两条红线，把「引用第三方库须保留其 LICENSE」写进宪法，避免再犯：

**规矩1.1 红线** 建议加一句：
> …素材必须标来源 + 许可；本项目自身代码默认 MIT 开源、可自由复用（注明来源即可，不限商用/非商用）；
> 引用第三方库（如 three.js / starflow）须保留其原有许可证与版权署名，不得剥离。

**规矩3.4 底线2** 建议改为：
> 禁止用无授权素材：每条必须标来源 + 许可；引用第三方库须保留其 LICENSE 与版权声明，不得剥离。

---

## 四、规矩11 理解核对（倒数第二条，用户要求我先确认是否明确）

**我的理解**：双轨预览 = 卡片 iframe 给降级版（2D/Canvas/file:// 可跑）并标「预览版」徽章；
用户拿到的代码/提示词/参数永远是完整版；降级版禁止塞完整 3D；3D 类给下载按钮；
入库须同时交降级版 + 完整版。

**三个我拿不准、需你拍板的点**（详见主会话）：
1. 参数对齐对象冲突：s209 的 14 参数键对齐的是完整版 state，但 iframe 加载降级版（只接了 2 个参数）→ 调参实时预览几乎无变化。规矩11 没说降级版要不要反映调参。
2. 完整版只可下载、不可在画廊预览 → 用户永远只看降级版调参。是否预期？
3. `效果演示` vs `ThreeJS演示` 字段语义：iframe 永远用 `效果演示`（降级），`ThreeJS演示` 仅用于下载/另开完整版？

---

## 五、仍待你决策的两处旧问题（原审查 Q1/Q2）

- **Q1（规矩5 说明标准漂移）**：宪法写「我的说明 = 三把尺子语言 + 能怎么改 + 满两行」，
  但 67 条实际都是 `效果说明`（一句话）+ `用法` 两字段，没一条符合字面。放松规矩5 还是补 67 条？
- **Q2（宪法内部冲突）**：规矩5「代码（可复制/库引用）」+ 3.3 红线「复制一行引用即可」
  vs 规矩10③「代码必须完整可运行（不是一句文件引用）」。建议给 10③ 加「引用第三方库时写完整调用代码+注明库路径，不算文件引用」例外。
