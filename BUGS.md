# BUGS · Web 灵感弹药库

> 本文件记录画廊 `index.html` 在真实使用中发现的交互/视觉缺陷与修复。
> 每次修复后必须：① 补一条记录 ② 跑 `node scripts/check_all.js` / 冒烟测试 ③ 更新本文件状态。

---

## 2026-09-24 · 弹药包换皮后的首轮修复

### 1. 主题切换误触发（严重）
- **现象**：点方案库的「移动端」筛选 chip、点卡片区域、甚至点页面普通空白处都会触发亮色/暗色主题切换。
- **影响**：用户无法稳定筛选/浏览，每次交互都可能被强制换肤。
- **原因**：`document.addEventListener("pointerdown", ..., true)` 在捕获阶段监听，closest 排除列表不够严格，且对动态生成的 `.chip` / `.filter` 按钮判断不可靠，导致几乎所有点击都被判定为"空白处"。
- **修复**：
  - 改为 `click` 冒泡监听；
  - 触发条件收紧为 `e.target` 必须是 `.stage-wrap / .dock / .grid / .app / #themeWipe` 这几个容器自身；
  - 再用 closest 二次排除内部的 button、input、card、chip 等交互元素。
- **状态**：已修复 ✅

### 2. 方案库卡片左侧 spine-label 遮挡演示
- **现象**：方案库卡片缩略图左上角有一条竖排文字"书脊标"，盖住 iframe 演示内容（如 S01 大色块分区）。
- **影响**：用户看不清方案效果，违反"先看效果文案其次"的用户偏好。
- **原因**：为追求"书店夜读"隐喻，把 `spine-label` 做成 absolute 定位直接压在 iframe 上。
- **修复**：移除 thumb 内的 `spine-label`，把风格名截断后作为普通 `tagpill` 放进 `card-top`，不再遮挡演示。
- **状态**：已修复 ✅

### 3. 页脚实时走时腕表无意义
- **现象**：页面底部有一个 canvas 画的小腕表，显示当前时间，占据一整行。
- **影响**：用户认为"无意义"，分散注意力，与弹药库核心任务无关。
- **原因**：为追求"腕表定制"设计语言而添加的氛围锚点，但对用户没有信息价值。
- **修复**：删除 stage-foot 内的 watch 区块、CSS 样式、JS 绘制函数与 `setInterval`。
- **状态**：已修复 ✅

### 4. 搜索框交互薄弱（优化项）
- **现象**：搜索框只有基础输入和清空，缺少快捷键聚焦、最近搜索、结果高亮等现代搜索体验。
- **影响**：高频操作效率低，用户无法快速复用搜索词。
- **修复**：
  - 参考 GitHub 全局搜索 `/` 聚焦、Apple HIG 搜索字段、Sera UI Command Search 等实现；
  - 新增 `/` 全局快捷键聚焦搜索框；
  - 新增最近搜索历史下拉（按素材库/方案库分别存储，最多 5 条，localStorage 持久化）；
  - 搜索结果标题匹配词高亮 `<mark>`；
  - 搜索框 focus 时显示 accent glow；
  - Esc：有内容时清空并保留焦点，无内容时失焦。
- **状态**：已修复 ✅

---

## 复盘规则（防止再犯）

1. **主题切换/全局快捷键等"全页面事件"必须用 e.target === 容器本身 + closest 双重保险**，禁止单靠 `pointerdown` 捕获阶段判断。
2. **任何覆盖在 iframe/演示图上的装饰元素必须可关闭或移出演示区**，"隐喻"不能遮挡核心内容。
3. **新增视觉装饰前先问：这个元素对用户完成当前任务有没有直接帮助？** 没有就删除或默认隐藏。
4. **搜索/筛选等高频控件按 GitHub / Apple HIG / shadcn 等成熟组件对齐**：快捷键、历史、清除、空态、高亮、44px 触摸目标。

---

## 经验沉淀（本次重构的方法论留存）

> 以下不是缺陷，是 2026-09-24 一整轮「弹药包换皮 → 玻璃工艺方向 → 设置面板/快捷键/账户预布局」过程中值得复用的做法与反思。

### E1. 设置面板 / 可重绑快捷键 / 账户预布局 接口契约
- **设置状态**：`settings` 对象（主题 / 强调色 / 减少动效 / 卡片密度 / 基础字号 / 铭牌显隐 / 侧栏常驻）+ `saveSettings()` 写 `localStorage`；`applySettings()` 统一落 DOM。
- **快捷键**：`SHORTCUTS` 动作注册表（label + 默认 binding）+ `keymap` 当前绑定（`DEFAULT_KEYMAP` 为固化默认，不展示、不提供预设选择 UI）+ `renderKeys()` 渲染 + `startCapture()` 点键帽录制。一键仅绑一用（自动清除其它动作的相同键）。**默认键 = 网页通用 + GitHub 风格融合**：`Ctrl/⌘+,` 设置 · `/` 搜索 · `1/2` 素材/方案库 · `t` 主题 · `c` 弹匣 · `r` 随机装填 · `?` 帮助 · `Esc` 关浮层。
- **账户预布局（Auth 对象）**：`user` 状态 + `login()/register()/logout()` 函数体目前用 `localStorage` 模拟；函数内留 `// TODO(后端)` fetch 接入点注释。**将来换真后端只改函数体、UI 不动**。设置面板「账户」分类参照 GitHub 账户设置布局（昵称 / 头像 / 安全·即将开放 / 登录设备占位）。

### E2. 搜索框调研结论（落地参考）
调研 GitHub 全局搜索 / Apple HIG 搜索字段 / Sera UI Command Search 后落地：
- `/` 全局快捷键聚焦（输入框内不抢）；
- 最近搜索历史下拉（素材库 / 方案库各存 5 条，`localStorage` 持久化）；
- 标题匹配词高亮 `<mark>`；
- focus 时 accent glow；Esc 有内容清空保焦点、无内容失焦。

### E3. jsdom 真实数据冒烟测试模板
- 用 `resources:"usable"` + `file://` 让 jsdom 自己加载 `data/*.js`（不必手动注入，避免作用域冲突）；
- 必须桩：`IntersectionObserver`、`HTMLCanvasElement.prototype.getContext`（返回 null 触发守卫）、`scrollTo`、`Element.prototype.scrollIntoView`；
- 断言：卡片总数 = 273 素材 + 46 方案 = 319、可见数 = 当前视图、0 运行时错误（`window.onerror` 收集）。
- 这是「卡片变空白」事故的兜底防线（对接规矩 28）。

### E4. 产品侧反思（预设过度设计）
用户要「一个合理默认」时，别给选项面板。本轮我先做了「弹药库默认 / 网页通用 / GitHub 风格」三预设选择 UI，用户明确说"不需要选来选去，直接预设"——**内部机制不该暴露成用户可见的选择**。同样：实时腕表、过期的新手引导文案，都是"我替用户做了他没要的决定"。凡拿不准要不要做/怎么做，先问（规矩 3），不替用户拍板。
